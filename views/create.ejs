<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css" rel="stylesheet" />
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <main class="w-full h-screen bg-zinc-100 font-['Helvetica']">
        <header class="flex px-10 justify-between py-5">
            <h1 class="text-2xl tracking-tight">Kaahatabook</h1>
            <a class="tracking-tight" href="/">Home</a>
        </header>
        <div class="px-10 hisaabs">
            <h3 class="capitalize text-2xl font-medium mb-5 mt-10 tracking-tight">Create New Hisaab</h3>
            <form action="/update/<%= Today %>.txt" method="post">
                <input name="title" class="text-md px-3 py-2 w-1/2 mb-5 rounded-md bg-zinc-200" type="text"
                    placeholder="Shopping ka hisaab, ghar ka kharch..." disabled>
                <textarea name="txt" class="block w-1/2 px-3 py-2 resize-none bg-zinc-200 rounded-md min-h-60"
                    placeholder="write your hisaab." id="txtArea"></textarea>
                <input type="submit" id="botton" class="block mt-5 px-5 py-3 bg-blue-500 text-white rounded-md"
                   value="Create Hisaab">
            </form>
        </div>
    </main>
    <script>
        const form = document.querySelector('form');
        form.addEventListener('submit', function(e) {
            const txtArea = document.getElementById('txtArea');
            if (txtArea.value.trim() === '') {
                e.preventDefault();
                alert('No Hisaab Written');
            } else {
                alert("Hisaab has been created");
            }
        });
    </script>
</body>


</html>