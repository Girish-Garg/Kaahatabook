<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css" rel="stylesheet" />
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <main class="w-full h-screen bg-zinc-100 font-['Helvetica']">
        <header class="flex px-10 justify-between py-5">
            <h1 class="text-2xl tracking-tight">Kaahatabook</h1>
            <div class="nav flex gap-5">
                <a class="tracking-tight" href="/">Home</a>
                <a class="tracking-tight" href="/create">Create New Hisaab</a>
            </div>
        </header>
        <div class="px-10">
            <h3 class="text-2xl font-medium mb-5 mt-10 tracking-tight">You're editing hisaab of <span
                    class="text-blue-500">
                    <% const date=fn.slice(0,-4) %>
                    <%= date %>
                </span>date</h3>
            <form action="/update/<%= fn %>" method="post">
                <textarea name="txt" class="block w-1/2 px-3 py-2 resize-none bg-zinc-200 rounded-md min-h-60 outline-none" placeholder="write your hisaab."><%= data %></textarea>
                <input class="block mt-5 px-5 py-3 bg-blue-500 text-white rounded-md" value="Update Hisaab" type="submit" id="botton">
            </form>
        </div>
    </main>
    <script>
        document.querySelector("#botton").addEventListener("click", () => {
            alert("Hisaab has been Updated");
        })
        console.log(data);
    </script>
</body>

</html>